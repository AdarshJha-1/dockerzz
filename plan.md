## Plan

### Step 1

-- I am thinking just to use raw commands execute them get result then show on screen (A simple print on screen type gui)

```go
type model struct {
    options []string
    selected int
    output string
    loading bool
    activeView string
}
```
